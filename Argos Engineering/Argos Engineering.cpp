#include <iostream>
#include "particle.h"
#include "point.h"
#include "surface.h"

using namespace std;

int main()
{
	/*surface sur(point(0, 0), point(10, 10), 1);
	sur.printLine();

	vector<particle> spawnedParticles = sur.spawnParticles(20);

	for (particle part : spawnedParticles) {
		part.printStatus();
	}*/
}